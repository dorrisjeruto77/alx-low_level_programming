#include <stdio.h>
#include "main.h"

/**
 * mul - multiplies two integers.
 * @b: input integer
 * @a: input integer
 * Return: 1 if c is uppercase and 0 otherwise.
 */

int mul(int a, int b)
{
    int value;

     value = a * b;

     return (value);
}
