#include <stdio.h>

/**
 * main - prints numbers of base 10 beggining from 0.
 * Return:0 (success)
 */

int main(void)

{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d", num);
	printf("\n");
return (0);
}
